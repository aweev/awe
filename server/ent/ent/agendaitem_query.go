// Code generated by ent, DO NOT EDIT.

package ent

import (
	"awe/ent/ent/actionitem"
	"awe/ent/ent/agenda"
	"awe/ent/ent/agendaitem"
	"awe/ent/ent/predicate"
	"awe/ent/ent/vote"
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// AgendaItemQuery is the builder for querying AgendaItem entities.
type AgendaItemQuery struct {
	config
	ctx             *QueryContext
	order           []agendaitem.OrderOption
	inters          []Interceptor
	predicates      []predicate.AgendaItem
	withAgenda      *AgendaQuery
	withVote        *VoteQuery
	withActionItems *ActionItemQuery
	withFKs         bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the AgendaItemQuery builder.
func (_q *AgendaItemQuery) Where(ps ...predicate.AgendaItem) *AgendaItemQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *AgendaItemQuery) Limit(limit int) *AgendaItemQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *AgendaItemQuery) Offset(offset int) *AgendaItemQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *AgendaItemQuery) Unique(unique bool) *AgendaItemQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *AgendaItemQuery) Order(o ...agendaitem.OrderOption) *AgendaItemQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryAgenda chains the current query on the "agenda" edge.
func (_q *AgendaItemQuery) QueryAgenda() *AgendaQuery {
	query := (&AgendaClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(agendaitem.Table, agendaitem.FieldID, selector),
			sqlgraph.To(agenda.Table, agenda.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, agendaitem.AgendaTable, agendaitem.AgendaColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryVote chains the current query on the "vote" edge.
func (_q *AgendaItemQuery) QueryVote() *VoteQuery {
	query := (&VoteClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(agendaitem.Table, agendaitem.FieldID, selector),
			sqlgraph.To(vote.Table, vote.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, false, agendaitem.VoteTable, agendaitem.VoteColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryActionItems chains the current query on the "action_items" edge.
func (_q *AgendaItemQuery) QueryActionItems() *ActionItemQuery {
	query := (&ActionItemClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(agendaitem.Table, agendaitem.FieldID, selector),
			sqlgraph.To(actionitem.Table, actionitem.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, agendaitem.ActionItemsTable, agendaitem.ActionItemsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first AgendaItem entity from the query.
// Returns a *NotFoundError when no AgendaItem was found.
func (_q *AgendaItemQuery) First(ctx context.Context) (*AgendaItem, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{agendaitem.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *AgendaItemQuery) FirstX(ctx context.Context) *AgendaItem {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first AgendaItem ID from the query.
// Returns a *NotFoundError when no AgendaItem ID was found.
func (_q *AgendaItemQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{agendaitem.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *AgendaItemQuery) FirstIDX(ctx context.Context) int {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single AgendaItem entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one AgendaItem entity is found.
// Returns a *NotFoundError when no AgendaItem entities are found.
func (_q *AgendaItemQuery) Only(ctx context.Context) (*AgendaItem, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{agendaitem.Label}
	default:
		return nil, &NotSingularError{agendaitem.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *AgendaItemQuery) OnlyX(ctx context.Context) *AgendaItem {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only AgendaItem ID in the query.
// Returns a *NotSingularError when more than one AgendaItem ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *AgendaItemQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{agendaitem.Label}
	default:
		err = &NotSingularError{agendaitem.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *AgendaItemQuery) OnlyIDX(ctx context.Context) int {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of AgendaItems.
func (_q *AgendaItemQuery) All(ctx context.Context) ([]*AgendaItem, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*AgendaItem, *AgendaItemQuery]()
	return withInterceptors[[]*AgendaItem](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *AgendaItemQuery) AllX(ctx context.Context) []*AgendaItem {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of AgendaItem IDs.
func (_q *AgendaItemQuery) IDs(ctx context.Context) (ids []int, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(agendaitem.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *AgendaItemQuery) IDsX(ctx context.Context) []int {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *AgendaItemQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*AgendaItemQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *AgendaItemQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *AgendaItemQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *AgendaItemQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the AgendaItemQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *AgendaItemQuery) Clone() *AgendaItemQuery {
	if _q == nil {
		return nil
	}
	return &AgendaItemQuery{
		config:          _q.config,
		ctx:             _q.ctx.Clone(),
		order:           append([]agendaitem.OrderOption{}, _q.order...),
		inters:          append([]Interceptor{}, _q.inters...),
		predicates:      append([]predicate.AgendaItem{}, _q.predicates...),
		withAgenda:      _q.withAgenda.Clone(),
		withVote:        _q.withVote.Clone(),
		withActionItems: _q.withActionItems.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithAgenda tells the query-builder to eager-load the nodes that are connected to
// the "agenda" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *AgendaItemQuery) WithAgenda(opts ...func(*AgendaQuery)) *AgendaItemQuery {
	query := (&AgendaClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withAgenda = query
	return _q
}

// WithVote tells the query-builder to eager-load the nodes that are connected to
// the "vote" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *AgendaItemQuery) WithVote(opts ...func(*VoteQuery)) *AgendaItemQuery {
	query := (&VoteClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withVote = query
	return _q
}

// WithActionItems tells the query-builder to eager-load the nodes that are connected to
// the "action_items" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *AgendaItemQuery) WithActionItems(opts ...func(*ActionItemQuery)) *AgendaItemQuery {
	query := (&ActionItemClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withActionItems = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Order int `json:"order,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.AgendaItem.Query().
//		GroupBy(agendaitem.FieldOrder).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *AgendaItemQuery) GroupBy(field string, fields ...string) *AgendaItemGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &AgendaItemGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = agendaitem.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Order int `json:"order,omitempty"`
//	}
//
//	client.AgendaItem.Query().
//		Select(agendaitem.FieldOrder).
//		Scan(ctx, &v)
func (_q *AgendaItemQuery) Select(fields ...string) *AgendaItemSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &AgendaItemSelect{AgendaItemQuery: _q}
	sbuild.label = agendaitem.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a AgendaItemSelect configured with the given aggregations.
func (_q *AgendaItemQuery) Aggregate(fns ...AggregateFunc) *AgendaItemSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *AgendaItemQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !agendaitem.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *AgendaItemQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*AgendaItem, error) {
	var (
		nodes       = []*AgendaItem{}
		withFKs     = _q.withFKs
		_spec       = _q.querySpec()
		loadedTypes = [3]bool{
			_q.withAgenda != nil,
			_q.withVote != nil,
			_q.withActionItems != nil,
		}
	)
	if _q.withAgenda != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, agendaitem.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*AgendaItem).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &AgendaItem{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withAgenda; query != nil {
		if err := _q.loadAgenda(ctx, query, nodes, nil,
			func(n *AgendaItem, e *Agenda) { n.Edges.Agenda = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withVote; query != nil {
		if err := _q.loadVote(ctx, query, nodes, nil,
			func(n *AgendaItem, e *Vote) { n.Edges.Vote = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withActionItems; query != nil {
		if err := _q.loadActionItems(ctx, query, nodes,
			func(n *AgendaItem) { n.Edges.ActionItems = []*ActionItem{} },
			func(n *AgendaItem, e *ActionItem) { n.Edges.ActionItems = append(n.Edges.ActionItems, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *AgendaItemQuery) loadAgenda(ctx context.Context, query *AgendaQuery, nodes []*AgendaItem, init func(*AgendaItem), assign func(*AgendaItem, *Agenda)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*AgendaItem)
	for i := range nodes {
		if nodes[i].agenda_items == nil {
			continue
		}
		fk := *nodes[i].agenda_items
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(agenda.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "agenda_items" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *AgendaItemQuery) loadVote(ctx context.Context, query *VoteQuery, nodes []*AgendaItem, init func(*AgendaItem), assign func(*AgendaItem, *Vote)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*AgendaItem)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
	}
	query.withFKs = true
	query.Where(predicate.Vote(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(agendaitem.VoteColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.agenda_item_vote
		if fk == nil {
			return fmt.Errorf(`foreign-key "agenda_item_vote" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "agenda_item_vote" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *AgendaItemQuery) loadActionItems(ctx context.Context, query *ActionItemQuery, nodes []*AgendaItem, init func(*AgendaItem), assign func(*AgendaItem, *ActionItem)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*AgendaItem)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.ActionItem(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(agendaitem.ActionItemsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.agenda_item_action_items
		if fk == nil {
			return fmt.Errorf(`foreign-key "agenda_item_action_items" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "agenda_item_action_items" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *AgendaItemQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *AgendaItemQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(agendaitem.Table, agendaitem.Columns, sqlgraph.NewFieldSpec(agendaitem.FieldID, field.TypeInt))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, agendaitem.FieldID)
		for i := range fields {
			if fields[i] != agendaitem.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *AgendaItemQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(agendaitem.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = agendaitem.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// AgendaItemGroupBy is the group-by builder for AgendaItem entities.
type AgendaItemGroupBy struct {
	selector
	build *AgendaItemQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *AgendaItemGroupBy) Aggregate(fns ...AggregateFunc) *AgendaItemGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *AgendaItemGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AgendaItemQuery, *AgendaItemGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *AgendaItemGroupBy) sqlScan(ctx context.Context, root *AgendaItemQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// AgendaItemSelect is the builder for selecting fields of AgendaItem entities.
type AgendaItemSelect struct {
	*AgendaItemQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *AgendaItemSelect) Aggregate(fns ...AggregateFunc) *AgendaItemSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *AgendaItemSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AgendaItemQuery, *AgendaItemSelect](ctx, _s.AgendaItemQuery, _s, _s.inters, v)
}

func (_s *AgendaItemSelect) sqlScan(ctx context.Context, root *AgendaItemQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
