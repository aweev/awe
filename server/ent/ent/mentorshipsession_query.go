// Code generated by ent, DO NOT EDIT.

package ent

import (
	"awe/ent/ent/agenda"
	"awe/ent/ent/mentorshiprequest"
	"awe/ent/ent/mentorshipsession"
	"awe/ent/ent/predicate"
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// MentorshipSessionQuery is the builder for querying MentorshipSession entities.
type MentorshipSessionQuery struct {
	config
	ctx                   *QueryContext
	order                 []mentorshipsession.OrderOption
	inters                []Interceptor
	predicates            []predicate.MentorshipSession
	withMentorshipRequest *MentorshipRequestQuery
	withAgenda            *AgendaQuery
	withFKs               bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the MentorshipSessionQuery builder.
func (_q *MentorshipSessionQuery) Where(ps ...predicate.MentorshipSession) *MentorshipSessionQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *MentorshipSessionQuery) Limit(limit int) *MentorshipSessionQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *MentorshipSessionQuery) Offset(offset int) *MentorshipSessionQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *MentorshipSessionQuery) Unique(unique bool) *MentorshipSessionQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *MentorshipSessionQuery) Order(o ...mentorshipsession.OrderOption) *MentorshipSessionQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryMentorshipRequest chains the current query on the "mentorship_request" edge.
func (_q *MentorshipSessionQuery) QueryMentorshipRequest() *MentorshipRequestQuery {
	query := (&MentorshipRequestClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(mentorshipsession.Table, mentorshipsession.FieldID, selector),
			sqlgraph.To(mentorshiprequest.Table, mentorshiprequest.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, mentorshipsession.MentorshipRequestTable, mentorshipsession.MentorshipRequestColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryAgenda chains the current query on the "agenda" edge.
func (_q *MentorshipSessionQuery) QueryAgenda() *AgendaQuery {
	query := (&AgendaClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(mentorshipsession.Table, mentorshipsession.FieldID, selector),
			sqlgraph.To(agenda.Table, agenda.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, false, mentorshipsession.AgendaTable, mentorshipsession.AgendaColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first MentorshipSession entity from the query.
// Returns a *NotFoundError when no MentorshipSession was found.
func (_q *MentorshipSessionQuery) First(ctx context.Context) (*MentorshipSession, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{mentorshipsession.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *MentorshipSessionQuery) FirstX(ctx context.Context) *MentorshipSession {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first MentorshipSession ID from the query.
// Returns a *NotFoundError when no MentorshipSession ID was found.
func (_q *MentorshipSessionQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{mentorshipsession.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *MentorshipSessionQuery) FirstIDX(ctx context.Context) int {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single MentorshipSession entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one MentorshipSession entity is found.
// Returns a *NotFoundError when no MentorshipSession entities are found.
func (_q *MentorshipSessionQuery) Only(ctx context.Context) (*MentorshipSession, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{mentorshipsession.Label}
	default:
		return nil, &NotSingularError{mentorshipsession.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *MentorshipSessionQuery) OnlyX(ctx context.Context) *MentorshipSession {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only MentorshipSession ID in the query.
// Returns a *NotSingularError when more than one MentorshipSession ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *MentorshipSessionQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{mentorshipsession.Label}
	default:
		err = &NotSingularError{mentorshipsession.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *MentorshipSessionQuery) OnlyIDX(ctx context.Context) int {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of MentorshipSessions.
func (_q *MentorshipSessionQuery) All(ctx context.Context) ([]*MentorshipSession, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*MentorshipSession, *MentorshipSessionQuery]()
	return withInterceptors[[]*MentorshipSession](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *MentorshipSessionQuery) AllX(ctx context.Context) []*MentorshipSession {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of MentorshipSession IDs.
func (_q *MentorshipSessionQuery) IDs(ctx context.Context) (ids []int, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(mentorshipsession.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *MentorshipSessionQuery) IDsX(ctx context.Context) []int {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *MentorshipSessionQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*MentorshipSessionQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *MentorshipSessionQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *MentorshipSessionQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *MentorshipSessionQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the MentorshipSessionQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *MentorshipSessionQuery) Clone() *MentorshipSessionQuery {
	if _q == nil {
		return nil
	}
	return &MentorshipSessionQuery{
		config:                _q.config,
		ctx:                   _q.ctx.Clone(),
		order:                 append([]mentorshipsession.OrderOption{}, _q.order...),
		inters:                append([]Interceptor{}, _q.inters...),
		predicates:            append([]predicate.MentorshipSession{}, _q.predicates...),
		withMentorshipRequest: _q.withMentorshipRequest.Clone(),
		withAgenda:            _q.withAgenda.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithMentorshipRequest tells the query-builder to eager-load the nodes that are connected to
// the "mentorship_request" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *MentorshipSessionQuery) WithMentorshipRequest(opts ...func(*MentorshipRequestQuery)) *MentorshipSessionQuery {
	query := (&MentorshipRequestClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withMentorshipRequest = query
	return _q
}

// WithAgenda tells the query-builder to eager-load the nodes that are connected to
// the "agenda" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *MentorshipSessionQuery) WithAgenda(opts ...func(*AgendaQuery)) *MentorshipSessionQuery {
	query := (&AgendaClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withAgenda = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ScheduledAt time.Time `json:"scheduled_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.MentorshipSession.Query().
//		GroupBy(mentorshipsession.FieldScheduledAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *MentorshipSessionQuery) GroupBy(field string, fields ...string) *MentorshipSessionGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &MentorshipSessionGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = mentorshipsession.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ScheduledAt time.Time `json:"scheduled_at,omitempty"`
//	}
//
//	client.MentorshipSession.Query().
//		Select(mentorshipsession.FieldScheduledAt).
//		Scan(ctx, &v)
func (_q *MentorshipSessionQuery) Select(fields ...string) *MentorshipSessionSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &MentorshipSessionSelect{MentorshipSessionQuery: _q}
	sbuild.label = mentorshipsession.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a MentorshipSessionSelect configured with the given aggregations.
func (_q *MentorshipSessionQuery) Aggregate(fns ...AggregateFunc) *MentorshipSessionSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *MentorshipSessionQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !mentorshipsession.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *MentorshipSessionQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*MentorshipSession, error) {
	var (
		nodes       = []*MentorshipSession{}
		withFKs     = _q.withFKs
		_spec       = _q.querySpec()
		loadedTypes = [2]bool{
			_q.withMentorshipRequest != nil,
			_q.withAgenda != nil,
		}
	)
	if _q.withMentorshipRequest != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, mentorshipsession.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*MentorshipSession).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &MentorshipSession{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withMentorshipRequest; query != nil {
		if err := _q.loadMentorshipRequest(ctx, query, nodes, nil,
			func(n *MentorshipSession, e *MentorshipRequest) { n.Edges.MentorshipRequest = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withAgenda; query != nil {
		if err := _q.loadAgenda(ctx, query, nodes, nil,
			func(n *MentorshipSession, e *Agenda) { n.Edges.Agenda = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *MentorshipSessionQuery) loadMentorshipRequest(ctx context.Context, query *MentorshipRequestQuery, nodes []*MentorshipSession, init func(*MentorshipSession), assign func(*MentorshipSession, *MentorshipRequest)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*MentorshipSession)
	for i := range nodes {
		if nodes[i].mentorship_request_sessions == nil {
			continue
		}
		fk := *nodes[i].mentorship_request_sessions
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(mentorshiprequest.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "mentorship_request_sessions" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *MentorshipSessionQuery) loadAgenda(ctx context.Context, query *AgendaQuery, nodes []*MentorshipSession, init func(*MentorshipSession), assign func(*MentorshipSession, *Agenda)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*MentorshipSession)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
	}
	query.withFKs = true
	query.Where(predicate.Agenda(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(mentorshipsession.AgendaColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.mentorship_session_agenda
		if fk == nil {
			return fmt.Errorf(`foreign-key "mentorship_session_agenda" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "mentorship_session_agenda" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *MentorshipSessionQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *MentorshipSessionQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(mentorshipsession.Table, mentorshipsession.Columns, sqlgraph.NewFieldSpec(mentorshipsession.FieldID, field.TypeInt))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, mentorshipsession.FieldID)
		for i := range fields {
			if fields[i] != mentorshipsession.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *MentorshipSessionQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(mentorshipsession.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = mentorshipsession.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// MentorshipSessionGroupBy is the group-by builder for MentorshipSession entities.
type MentorshipSessionGroupBy struct {
	selector
	build *MentorshipSessionQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *MentorshipSessionGroupBy) Aggregate(fns ...AggregateFunc) *MentorshipSessionGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *MentorshipSessionGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MentorshipSessionQuery, *MentorshipSessionGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *MentorshipSessionGroupBy) sqlScan(ctx context.Context, root *MentorshipSessionQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// MentorshipSessionSelect is the builder for selecting fields of MentorshipSession entities.
type MentorshipSessionSelect struct {
	*MentorshipSessionQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *MentorshipSessionSelect) Aggregate(fns ...AggregateFunc) *MentorshipSessionSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *MentorshipSessionSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MentorshipSessionQuery, *MentorshipSessionSelect](ctx, _s.MentorshipSessionQuery, _s, _s.inters, v)
}

func (_s *MentorshipSessionSelect) sqlScan(ctx context.Context, root *MentorshipSessionQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
