// Code generated by ent, DO NOT EDIT.

package ent

import (
	"awe/ent/ent/mentorshiprequest"
	"awe/ent/ent/mentorshipsession"
	"awe/ent/ent/predicate"
	"awe/ent/ent/user"
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// MentorshipRequestQuery is the builder for querying MentorshipRequest entities.
type MentorshipRequestQuery struct {
	config
	ctx           *QueryContext
	order         []mentorshiprequest.OrderOption
	inters        []Interceptor
	predicates    []predicate.MentorshipRequest
	withRequester *UserQuery
	withMentor    *UserQuery
	withSessions  *MentorshipSessionQuery
	withFKs       bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the MentorshipRequestQuery builder.
func (_q *MentorshipRequestQuery) Where(ps ...predicate.MentorshipRequest) *MentorshipRequestQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *MentorshipRequestQuery) Limit(limit int) *MentorshipRequestQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *MentorshipRequestQuery) Offset(offset int) *MentorshipRequestQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *MentorshipRequestQuery) Unique(unique bool) *MentorshipRequestQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *MentorshipRequestQuery) Order(o ...mentorshiprequest.OrderOption) *MentorshipRequestQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryRequester chains the current query on the "requester" edge.
func (_q *MentorshipRequestQuery) QueryRequester() *UserQuery {
	query := (&UserClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(mentorshiprequest.Table, mentorshiprequest.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, mentorshiprequest.RequesterTable, mentorshiprequest.RequesterColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryMentor chains the current query on the "mentor" edge.
func (_q *MentorshipRequestQuery) QueryMentor() *UserQuery {
	query := (&UserClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(mentorshiprequest.Table, mentorshiprequest.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, mentorshiprequest.MentorTable, mentorshiprequest.MentorColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QuerySessions chains the current query on the "sessions" edge.
func (_q *MentorshipRequestQuery) QuerySessions() *MentorshipSessionQuery {
	query := (&MentorshipSessionClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(mentorshiprequest.Table, mentorshiprequest.FieldID, selector),
			sqlgraph.To(mentorshipsession.Table, mentorshipsession.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, mentorshiprequest.SessionsTable, mentorshiprequest.SessionsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first MentorshipRequest entity from the query.
// Returns a *NotFoundError when no MentorshipRequest was found.
func (_q *MentorshipRequestQuery) First(ctx context.Context) (*MentorshipRequest, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{mentorshiprequest.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *MentorshipRequestQuery) FirstX(ctx context.Context) *MentorshipRequest {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first MentorshipRequest ID from the query.
// Returns a *NotFoundError when no MentorshipRequest ID was found.
func (_q *MentorshipRequestQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{mentorshiprequest.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *MentorshipRequestQuery) FirstIDX(ctx context.Context) int {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single MentorshipRequest entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one MentorshipRequest entity is found.
// Returns a *NotFoundError when no MentorshipRequest entities are found.
func (_q *MentorshipRequestQuery) Only(ctx context.Context) (*MentorshipRequest, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{mentorshiprequest.Label}
	default:
		return nil, &NotSingularError{mentorshiprequest.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *MentorshipRequestQuery) OnlyX(ctx context.Context) *MentorshipRequest {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only MentorshipRequest ID in the query.
// Returns a *NotSingularError when more than one MentorshipRequest ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *MentorshipRequestQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{mentorshiprequest.Label}
	default:
		err = &NotSingularError{mentorshiprequest.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *MentorshipRequestQuery) OnlyIDX(ctx context.Context) int {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of MentorshipRequests.
func (_q *MentorshipRequestQuery) All(ctx context.Context) ([]*MentorshipRequest, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*MentorshipRequest, *MentorshipRequestQuery]()
	return withInterceptors[[]*MentorshipRequest](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *MentorshipRequestQuery) AllX(ctx context.Context) []*MentorshipRequest {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of MentorshipRequest IDs.
func (_q *MentorshipRequestQuery) IDs(ctx context.Context) (ids []int, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(mentorshiprequest.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *MentorshipRequestQuery) IDsX(ctx context.Context) []int {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *MentorshipRequestQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*MentorshipRequestQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *MentorshipRequestQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *MentorshipRequestQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *MentorshipRequestQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the MentorshipRequestQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *MentorshipRequestQuery) Clone() *MentorshipRequestQuery {
	if _q == nil {
		return nil
	}
	return &MentorshipRequestQuery{
		config:        _q.config,
		ctx:           _q.ctx.Clone(),
		order:         append([]mentorshiprequest.OrderOption{}, _q.order...),
		inters:        append([]Interceptor{}, _q.inters...),
		predicates:    append([]predicate.MentorshipRequest{}, _q.predicates...),
		withRequester: _q.withRequester.Clone(),
		withMentor:    _q.withMentor.Clone(),
		withSessions:  _q.withSessions.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithRequester tells the query-builder to eager-load the nodes that are connected to
// the "requester" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *MentorshipRequestQuery) WithRequester(opts ...func(*UserQuery)) *MentorshipRequestQuery {
	query := (&UserClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withRequester = query
	return _q
}

// WithMentor tells the query-builder to eager-load the nodes that are connected to
// the "mentor" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *MentorshipRequestQuery) WithMentor(opts ...func(*UserQuery)) *MentorshipRequestQuery {
	query := (&UserClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withMentor = query
	return _q
}

// WithSessions tells the query-builder to eager-load the nodes that are connected to
// the "sessions" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *MentorshipRequestQuery) WithSessions(opts ...func(*MentorshipSessionQuery)) *MentorshipRequestQuery {
	query := (&MentorshipSessionClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withSessions = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Status mentorshiprequest.Status `json:"status,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.MentorshipRequest.Query().
//		GroupBy(mentorshiprequest.FieldStatus).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *MentorshipRequestQuery) GroupBy(field string, fields ...string) *MentorshipRequestGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &MentorshipRequestGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = mentorshiprequest.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Status mentorshiprequest.Status `json:"status,omitempty"`
//	}
//
//	client.MentorshipRequest.Query().
//		Select(mentorshiprequest.FieldStatus).
//		Scan(ctx, &v)
func (_q *MentorshipRequestQuery) Select(fields ...string) *MentorshipRequestSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &MentorshipRequestSelect{MentorshipRequestQuery: _q}
	sbuild.label = mentorshiprequest.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a MentorshipRequestSelect configured with the given aggregations.
func (_q *MentorshipRequestQuery) Aggregate(fns ...AggregateFunc) *MentorshipRequestSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *MentorshipRequestQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !mentorshiprequest.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *MentorshipRequestQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*MentorshipRequest, error) {
	var (
		nodes       = []*MentorshipRequest{}
		withFKs     = _q.withFKs
		_spec       = _q.querySpec()
		loadedTypes = [3]bool{
			_q.withRequester != nil,
			_q.withMentor != nil,
			_q.withSessions != nil,
		}
	)
	if _q.withRequester != nil || _q.withMentor != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, mentorshiprequest.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*MentorshipRequest).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &MentorshipRequest{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withRequester; query != nil {
		if err := _q.loadRequester(ctx, query, nodes, nil,
			func(n *MentorshipRequest, e *User) { n.Edges.Requester = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withMentor; query != nil {
		if err := _q.loadMentor(ctx, query, nodes, nil,
			func(n *MentorshipRequest, e *User) { n.Edges.Mentor = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withSessions; query != nil {
		if err := _q.loadSessions(ctx, query, nodes,
			func(n *MentorshipRequest) { n.Edges.Sessions = []*MentorshipSession{} },
			func(n *MentorshipRequest, e *MentorshipSession) { n.Edges.Sessions = append(n.Edges.Sessions, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *MentorshipRequestQuery) loadRequester(ctx context.Context, query *UserQuery, nodes []*MentorshipRequest, init func(*MentorshipRequest), assign func(*MentorshipRequest, *User)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*MentorshipRequest)
	for i := range nodes {
		if nodes[i].user_mentorship_requests == nil {
			continue
		}
		fk := *nodes[i].user_mentorship_requests
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_mentorship_requests" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *MentorshipRequestQuery) loadMentor(ctx context.Context, query *UserQuery, nodes []*MentorshipRequest, init func(*MentorshipRequest), assign func(*MentorshipRequest, *User)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*MentorshipRequest)
	for i := range nodes {
		if nodes[i].user_mentorships_mentoring == nil {
			continue
		}
		fk := *nodes[i].user_mentorships_mentoring
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_mentorships_mentoring" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *MentorshipRequestQuery) loadSessions(ctx context.Context, query *MentorshipSessionQuery, nodes []*MentorshipRequest, init func(*MentorshipRequest), assign func(*MentorshipRequest, *MentorshipSession)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*MentorshipRequest)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.MentorshipSession(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(mentorshiprequest.SessionsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.mentorship_request_sessions
		if fk == nil {
			return fmt.Errorf(`foreign-key "mentorship_request_sessions" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "mentorship_request_sessions" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *MentorshipRequestQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *MentorshipRequestQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(mentorshiprequest.Table, mentorshiprequest.Columns, sqlgraph.NewFieldSpec(mentorshiprequest.FieldID, field.TypeInt))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, mentorshiprequest.FieldID)
		for i := range fields {
			if fields[i] != mentorshiprequest.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *MentorshipRequestQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(mentorshiprequest.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = mentorshiprequest.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// MentorshipRequestGroupBy is the group-by builder for MentorshipRequest entities.
type MentorshipRequestGroupBy struct {
	selector
	build *MentorshipRequestQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *MentorshipRequestGroupBy) Aggregate(fns ...AggregateFunc) *MentorshipRequestGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *MentorshipRequestGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MentorshipRequestQuery, *MentorshipRequestGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *MentorshipRequestGroupBy) sqlScan(ctx context.Context, root *MentorshipRequestQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// MentorshipRequestSelect is the builder for selecting fields of MentorshipRequest entities.
type MentorshipRequestSelect struct {
	*MentorshipRequestQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *MentorshipRequestSelect) Aggregate(fns ...AggregateFunc) *MentorshipRequestSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *MentorshipRequestSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MentorshipRequestQuery, *MentorshipRequestSelect](ctx, _s.MentorshipRequestQuery, _s, _s.inters, v)
}

func (_s *MentorshipRequestSelect) sqlScan(ctx context.Context, root *MentorshipRequestQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
