// Code generated by ent, DO NOT EDIT.

package ent

import (
	"awe/ent/ent/donation"
	"awe/ent/ent/fundraisingcampaign"
	"awe/ent/ent/predicate"
	"awe/ent/ent/program"
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// FundraisingCampaignQuery is the builder for querying FundraisingCampaign entities.
type FundraisingCampaignQuery struct {
	config
	ctx                *QueryContext
	order              []fundraisingcampaign.OrderOption
	inters             []Interceptor
	predicates         []predicate.FundraisingCampaign
	withProgram        *ProgramQuery
	withChildCampaigns *FundraisingCampaignQuery
	withParentCampaign *FundraisingCampaignQuery
	withDonations      *DonationQuery
	withFKs            bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the FundraisingCampaignQuery builder.
func (_q *FundraisingCampaignQuery) Where(ps ...predicate.FundraisingCampaign) *FundraisingCampaignQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *FundraisingCampaignQuery) Limit(limit int) *FundraisingCampaignQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *FundraisingCampaignQuery) Offset(offset int) *FundraisingCampaignQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *FundraisingCampaignQuery) Unique(unique bool) *FundraisingCampaignQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *FundraisingCampaignQuery) Order(o ...fundraisingcampaign.OrderOption) *FundraisingCampaignQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryProgram chains the current query on the "program" edge.
func (_q *FundraisingCampaignQuery) QueryProgram() *ProgramQuery {
	query := (&ProgramClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(fundraisingcampaign.Table, fundraisingcampaign.FieldID, selector),
			sqlgraph.To(program.Table, program.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, fundraisingcampaign.ProgramTable, fundraisingcampaign.ProgramColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryChildCampaigns chains the current query on the "child_campaigns" edge.
func (_q *FundraisingCampaignQuery) QueryChildCampaigns() *FundraisingCampaignQuery {
	query := (&FundraisingCampaignClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(fundraisingcampaign.Table, fundraisingcampaign.FieldID, selector),
			sqlgraph.To(fundraisingcampaign.Table, fundraisingcampaign.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, true, fundraisingcampaign.ChildCampaignsTable, fundraisingcampaign.ChildCampaignsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryParentCampaign chains the current query on the "parent_campaign" edge.
func (_q *FundraisingCampaignQuery) QueryParentCampaign() *FundraisingCampaignQuery {
	query := (&FundraisingCampaignClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(fundraisingcampaign.Table, fundraisingcampaign.FieldID, selector),
			sqlgraph.To(fundraisingcampaign.Table, fundraisingcampaign.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, fundraisingcampaign.ParentCampaignTable, fundraisingcampaign.ParentCampaignColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryDonations chains the current query on the "donations" edge.
func (_q *FundraisingCampaignQuery) QueryDonations() *DonationQuery {
	query := (&DonationClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(fundraisingcampaign.Table, fundraisingcampaign.FieldID, selector),
			sqlgraph.To(donation.Table, donation.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, fundraisingcampaign.DonationsTable, fundraisingcampaign.DonationsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first FundraisingCampaign entity from the query.
// Returns a *NotFoundError when no FundraisingCampaign was found.
func (_q *FundraisingCampaignQuery) First(ctx context.Context) (*FundraisingCampaign, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{fundraisingcampaign.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *FundraisingCampaignQuery) FirstX(ctx context.Context) *FundraisingCampaign {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first FundraisingCampaign ID from the query.
// Returns a *NotFoundError when no FundraisingCampaign ID was found.
func (_q *FundraisingCampaignQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{fundraisingcampaign.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *FundraisingCampaignQuery) FirstIDX(ctx context.Context) int {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single FundraisingCampaign entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one FundraisingCampaign entity is found.
// Returns a *NotFoundError when no FundraisingCampaign entities are found.
func (_q *FundraisingCampaignQuery) Only(ctx context.Context) (*FundraisingCampaign, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{fundraisingcampaign.Label}
	default:
		return nil, &NotSingularError{fundraisingcampaign.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *FundraisingCampaignQuery) OnlyX(ctx context.Context) *FundraisingCampaign {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only FundraisingCampaign ID in the query.
// Returns a *NotSingularError when more than one FundraisingCampaign ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *FundraisingCampaignQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{fundraisingcampaign.Label}
	default:
		err = &NotSingularError{fundraisingcampaign.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *FundraisingCampaignQuery) OnlyIDX(ctx context.Context) int {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of FundraisingCampaigns.
func (_q *FundraisingCampaignQuery) All(ctx context.Context) ([]*FundraisingCampaign, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*FundraisingCampaign, *FundraisingCampaignQuery]()
	return withInterceptors[[]*FundraisingCampaign](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *FundraisingCampaignQuery) AllX(ctx context.Context) []*FundraisingCampaign {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of FundraisingCampaign IDs.
func (_q *FundraisingCampaignQuery) IDs(ctx context.Context) (ids []int, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(fundraisingcampaign.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *FundraisingCampaignQuery) IDsX(ctx context.Context) []int {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *FundraisingCampaignQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*FundraisingCampaignQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *FundraisingCampaignQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *FundraisingCampaignQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *FundraisingCampaignQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the FundraisingCampaignQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *FundraisingCampaignQuery) Clone() *FundraisingCampaignQuery {
	if _q == nil {
		return nil
	}
	return &FundraisingCampaignQuery{
		config:             _q.config,
		ctx:                _q.ctx.Clone(),
		order:              append([]fundraisingcampaign.OrderOption{}, _q.order...),
		inters:             append([]Interceptor{}, _q.inters...),
		predicates:         append([]predicate.FundraisingCampaign{}, _q.predicates...),
		withProgram:        _q.withProgram.Clone(),
		withChildCampaigns: _q.withChildCampaigns.Clone(),
		withParentCampaign: _q.withParentCampaign.Clone(),
		withDonations:      _q.withDonations.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithProgram tells the query-builder to eager-load the nodes that are connected to
// the "program" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *FundraisingCampaignQuery) WithProgram(opts ...func(*ProgramQuery)) *FundraisingCampaignQuery {
	query := (&ProgramClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withProgram = query
	return _q
}

// WithChildCampaigns tells the query-builder to eager-load the nodes that are connected to
// the "child_campaigns" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *FundraisingCampaignQuery) WithChildCampaigns(opts ...func(*FundraisingCampaignQuery)) *FundraisingCampaignQuery {
	query := (&FundraisingCampaignClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withChildCampaigns = query
	return _q
}

// WithParentCampaign tells the query-builder to eager-load the nodes that are connected to
// the "parent_campaign" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *FundraisingCampaignQuery) WithParentCampaign(opts ...func(*FundraisingCampaignQuery)) *FundraisingCampaignQuery {
	query := (&FundraisingCampaignClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withParentCampaign = query
	return _q
}

// WithDonations tells the query-builder to eager-load the nodes that are connected to
// the "donations" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *FundraisingCampaignQuery) WithDonations(opts ...func(*DonationQuery)) *FundraisingCampaignQuery {
	query := (&DonationClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withDonations = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Slug string `json:"slug,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.FundraisingCampaign.Query().
//		GroupBy(fundraisingcampaign.FieldSlug).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *FundraisingCampaignQuery) GroupBy(field string, fields ...string) *FundraisingCampaignGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &FundraisingCampaignGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = fundraisingcampaign.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Slug string `json:"slug,omitempty"`
//	}
//
//	client.FundraisingCampaign.Query().
//		Select(fundraisingcampaign.FieldSlug).
//		Scan(ctx, &v)
func (_q *FundraisingCampaignQuery) Select(fields ...string) *FundraisingCampaignSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &FundraisingCampaignSelect{FundraisingCampaignQuery: _q}
	sbuild.label = fundraisingcampaign.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a FundraisingCampaignSelect configured with the given aggregations.
func (_q *FundraisingCampaignQuery) Aggregate(fns ...AggregateFunc) *FundraisingCampaignSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *FundraisingCampaignQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !fundraisingcampaign.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *FundraisingCampaignQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*FundraisingCampaign, error) {
	var (
		nodes       = []*FundraisingCampaign{}
		withFKs     = _q.withFKs
		_spec       = _q.querySpec()
		loadedTypes = [4]bool{
			_q.withProgram != nil,
			_q.withChildCampaigns != nil,
			_q.withParentCampaign != nil,
			_q.withDonations != nil,
		}
	)
	if _q.withProgram != nil || _q.withParentCampaign != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, fundraisingcampaign.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*FundraisingCampaign).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &FundraisingCampaign{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withProgram; query != nil {
		if err := _q.loadProgram(ctx, query, nodes, nil,
			func(n *FundraisingCampaign, e *Program) { n.Edges.Program = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withChildCampaigns; query != nil {
		if err := _q.loadChildCampaigns(ctx, query, nodes,
			func(n *FundraisingCampaign) { n.Edges.ChildCampaigns = []*FundraisingCampaign{} },
			func(n *FundraisingCampaign, e *FundraisingCampaign) {
				n.Edges.ChildCampaigns = append(n.Edges.ChildCampaigns, e)
			}); err != nil {
			return nil, err
		}
	}
	if query := _q.withParentCampaign; query != nil {
		if err := _q.loadParentCampaign(ctx, query, nodes, nil,
			func(n *FundraisingCampaign, e *FundraisingCampaign) { n.Edges.ParentCampaign = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withDonations; query != nil {
		if err := _q.loadDonations(ctx, query, nodes,
			func(n *FundraisingCampaign) { n.Edges.Donations = []*Donation{} },
			func(n *FundraisingCampaign, e *Donation) { n.Edges.Donations = append(n.Edges.Donations, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *FundraisingCampaignQuery) loadProgram(ctx context.Context, query *ProgramQuery, nodes []*FundraisingCampaign, init func(*FundraisingCampaign), assign func(*FundraisingCampaign, *Program)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*FundraisingCampaign)
	for i := range nodes {
		if nodes[i].program_fundraising_campaigns == nil {
			continue
		}
		fk := *nodes[i].program_fundraising_campaigns
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(program.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "program_fundraising_campaigns" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *FundraisingCampaignQuery) loadChildCampaigns(ctx context.Context, query *FundraisingCampaignQuery, nodes []*FundraisingCampaign, init func(*FundraisingCampaign), assign func(*FundraisingCampaign, *FundraisingCampaign)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*FundraisingCampaign)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.FundraisingCampaign(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(fundraisingcampaign.ChildCampaignsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.fundraising_campaign_parent_campaign
		if fk == nil {
			return fmt.Errorf(`foreign-key "fundraising_campaign_parent_campaign" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "fundraising_campaign_parent_campaign" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *FundraisingCampaignQuery) loadParentCampaign(ctx context.Context, query *FundraisingCampaignQuery, nodes []*FundraisingCampaign, init func(*FundraisingCampaign), assign func(*FundraisingCampaign, *FundraisingCampaign)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*FundraisingCampaign)
	for i := range nodes {
		if nodes[i].fundraising_campaign_parent_campaign == nil {
			continue
		}
		fk := *nodes[i].fundraising_campaign_parent_campaign
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(fundraisingcampaign.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "fundraising_campaign_parent_campaign" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *FundraisingCampaignQuery) loadDonations(ctx context.Context, query *DonationQuery, nodes []*FundraisingCampaign, init func(*FundraisingCampaign), assign func(*FundraisingCampaign, *Donation)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*FundraisingCampaign)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.Donation(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(fundraisingcampaign.DonationsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.fundraising_campaign_donations
		if fk == nil {
			return fmt.Errorf(`foreign-key "fundraising_campaign_donations" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "fundraising_campaign_donations" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *FundraisingCampaignQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *FundraisingCampaignQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(fundraisingcampaign.Table, fundraisingcampaign.Columns, sqlgraph.NewFieldSpec(fundraisingcampaign.FieldID, field.TypeInt))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, fundraisingcampaign.FieldID)
		for i := range fields {
			if fields[i] != fundraisingcampaign.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *FundraisingCampaignQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(fundraisingcampaign.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = fundraisingcampaign.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// FundraisingCampaignGroupBy is the group-by builder for FundraisingCampaign entities.
type FundraisingCampaignGroupBy struct {
	selector
	build *FundraisingCampaignQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *FundraisingCampaignGroupBy) Aggregate(fns ...AggregateFunc) *FundraisingCampaignGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *FundraisingCampaignGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FundraisingCampaignQuery, *FundraisingCampaignGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *FundraisingCampaignGroupBy) sqlScan(ctx context.Context, root *FundraisingCampaignQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// FundraisingCampaignSelect is the builder for selecting fields of FundraisingCampaign entities.
type FundraisingCampaignSelect struct {
	*FundraisingCampaignQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *FundraisingCampaignSelect) Aggregate(fns ...AggregateFunc) *FundraisingCampaignSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *FundraisingCampaignSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FundraisingCampaignQuery, *FundraisingCampaignSelect](ctx, _s.FundraisingCampaignQuery, _s, _s.inters, v)
}

func (_s *FundraisingCampaignSelect) sqlScan(ctx context.Context, root *FundraisingCampaignQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
