// Code generated by ent, DO NOT EDIT.

package ent

import (
	"awe/ent/ent/blocktimeline"
	"awe/ent/ent/contentblock"
	"awe/ent/ent/predicate"
	"awe/ent/ent/timelineevent"
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// BlockTimelineQuery is the builder for querying BlockTimeline entities.
type BlockTimelineQuery struct {
	config
	ctx              *QueryContext
	order            []blocktimeline.OrderOption
	inters           []Interceptor
	predicates       []predicate.BlockTimeline
	withContentBlock *ContentBlockQuery
	withEvents       *TimelineEventQuery
	withFKs          bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the BlockTimelineQuery builder.
func (_q *BlockTimelineQuery) Where(ps ...predicate.BlockTimeline) *BlockTimelineQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *BlockTimelineQuery) Limit(limit int) *BlockTimelineQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *BlockTimelineQuery) Offset(offset int) *BlockTimelineQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *BlockTimelineQuery) Unique(unique bool) *BlockTimelineQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *BlockTimelineQuery) Order(o ...blocktimeline.OrderOption) *BlockTimelineQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryContentBlock chains the current query on the "content_block" edge.
func (_q *BlockTimelineQuery) QueryContentBlock() *ContentBlockQuery {
	query := (&ContentBlockClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(blocktimeline.Table, blocktimeline.FieldID, selector),
			sqlgraph.To(contentblock.Table, contentblock.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, blocktimeline.ContentBlockTable, blocktimeline.ContentBlockColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryEvents chains the current query on the "events" edge.
func (_q *BlockTimelineQuery) QueryEvents() *TimelineEventQuery {
	query := (&TimelineEventClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(blocktimeline.Table, blocktimeline.FieldID, selector),
			sqlgraph.To(timelineevent.Table, timelineevent.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, blocktimeline.EventsTable, blocktimeline.EventsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first BlockTimeline entity from the query.
// Returns a *NotFoundError when no BlockTimeline was found.
func (_q *BlockTimelineQuery) First(ctx context.Context) (*BlockTimeline, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{blocktimeline.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *BlockTimelineQuery) FirstX(ctx context.Context) *BlockTimeline {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first BlockTimeline ID from the query.
// Returns a *NotFoundError when no BlockTimeline ID was found.
func (_q *BlockTimelineQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{blocktimeline.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *BlockTimelineQuery) FirstIDX(ctx context.Context) int {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single BlockTimeline entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one BlockTimeline entity is found.
// Returns a *NotFoundError when no BlockTimeline entities are found.
func (_q *BlockTimelineQuery) Only(ctx context.Context) (*BlockTimeline, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{blocktimeline.Label}
	default:
		return nil, &NotSingularError{blocktimeline.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *BlockTimelineQuery) OnlyX(ctx context.Context) *BlockTimeline {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only BlockTimeline ID in the query.
// Returns a *NotSingularError when more than one BlockTimeline ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *BlockTimelineQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{blocktimeline.Label}
	default:
		err = &NotSingularError{blocktimeline.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *BlockTimelineQuery) OnlyIDX(ctx context.Context) int {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of BlockTimelines.
func (_q *BlockTimelineQuery) All(ctx context.Context) ([]*BlockTimeline, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*BlockTimeline, *BlockTimelineQuery]()
	return withInterceptors[[]*BlockTimeline](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *BlockTimelineQuery) AllX(ctx context.Context) []*BlockTimeline {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of BlockTimeline IDs.
func (_q *BlockTimelineQuery) IDs(ctx context.Context) (ids []int, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(blocktimeline.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *BlockTimelineQuery) IDsX(ctx context.Context) []int {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *BlockTimelineQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*BlockTimelineQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *BlockTimelineQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *BlockTimelineQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *BlockTimelineQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the BlockTimelineQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *BlockTimelineQuery) Clone() *BlockTimelineQuery {
	if _q == nil {
		return nil
	}
	return &BlockTimelineQuery{
		config:           _q.config,
		ctx:              _q.ctx.Clone(),
		order:            append([]blocktimeline.OrderOption{}, _q.order...),
		inters:           append([]Interceptor{}, _q.inters...),
		predicates:       append([]predicate.BlockTimeline{}, _q.predicates...),
		withContentBlock: _q.withContentBlock.Clone(),
		withEvents:       _q.withEvents.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithContentBlock tells the query-builder to eager-load the nodes that are connected to
// the "content_block" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *BlockTimelineQuery) WithContentBlock(opts ...func(*ContentBlockQuery)) *BlockTimelineQuery {
	query := (&ContentBlockClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withContentBlock = query
	return _q
}

// WithEvents tells the query-builder to eager-load the nodes that are connected to
// the "events" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *BlockTimelineQuery) WithEvents(opts ...func(*TimelineEventQuery)) *BlockTimelineQuery {
	query := (&TimelineEventClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withEvents = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		InternalName string `json:"internal_name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.BlockTimeline.Query().
//		GroupBy(blocktimeline.FieldInternalName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *BlockTimelineQuery) GroupBy(field string, fields ...string) *BlockTimelineGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &BlockTimelineGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = blocktimeline.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		InternalName string `json:"internal_name,omitempty"`
//	}
//
//	client.BlockTimeline.Query().
//		Select(blocktimeline.FieldInternalName).
//		Scan(ctx, &v)
func (_q *BlockTimelineQuery) Select(fields ...string) *BlockTimelineSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &BlockTimelineSelect{BlockTimelineQuery: _q}
	sbuild.label = blocktimeline.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a BlockTimelineSelect configured with the given aggregations.
func (_q *BlockTimelineQuery) Aggregate(fns ...AggregateFunc) *BlockTimelineSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *BlockTimelineQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !blocktimeline.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *BlockTimelineQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*BlockTimeline, error) {
	var (
		nodes       = []*BlockTimeline{}
		withFKs     = _q.withFKs
		_spec       = _q.querySpec()
		loadedTypes = [2]bool{
			_q.withContentBlock != nil,
			_q.withEvents != nil,
		}
	)
	if _q.withContentBlock != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, blocktimeline.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*BlockTimeline).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &BlockTimeline{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withContentBlock; query != nil {
		if err := _q.loadContentBlock(ctx, query, nodes, nil,
			func(n *BlockTimeline, e *ContentBlock) { n.Edges.ContentBlock = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withEvents; query != nil {
		if err := _q.loadEvents(ctx, query, nodes,
			func(n *BlockTimeline) { n.Edges.Events = []*TimelineEvent{} },
			func(n *BlockTimeline, e *TimelineEvent) { n.Edges.Events = append(n.Edges.Events, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *BlockTimelineQuery) loadContentBlock(ctx context.Context, query *ContentBlockQuery, nodes []*BlockTimeline, init func(*BlockTimeline), assign func(*BlockTimeline, *ContentBlock)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*BlockTimeline)
	for i := range nodes {
		if nodes[i].content_block_block_timeline == nil {
			continue
		}
		fk := *nodes[i].content_block_block_timeline
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(contentblock.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "content_block_block_timeline" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *BlockTimelineQuery) loadEvents(ctx context.Context, query *TimelineEventQuery, nodes []*BlockTimeline, init func(*BlockTimeline), assign func(*BlockTimeline, *TimelineEvent)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*BlockTimeline)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.TimelineEvent(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(blocktimeline.EventsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.block_timeline_events
		if fk == nil {
			return fmt.Errorf(`foreign-key "block_timeline_events" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "block_timeline_events" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *BlockTimelineQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *BlockTimelineQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(blocktimeline.Table, blocktimeline.Columns, sqlgraph.NewFieldSpec(blocktimeline.FieldID, field.TypeInt))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, blocktimeline.FieldID)
		for i := range fields {
			if fields[i] != blocktimeline.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *BlockTimelineQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(blocktimeline.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = blocktimeline.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// BlockTimelineGroupBy is the group-by builder for BlockTimeline entities.
type BlockTimelineGroupBy struct {
	selector
	build *BlockTimelineQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *BlockTimelineGroupBy) Aggregate(fns ...AggregateFunc) *BlockTimelineGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *BlockTimelineGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*BlockTimelineQuery, *BlockTimelineGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *BlockTimelineGroupBy) sqlScan(ctx context.Context, root *BlockTimelineQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// BlockTimelineSelect is the builder for selecting fields of BlockTimeline entities.
type BlockTimelineSelect struct {
	*BlockTimelineQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *BlockTimelineSelect) Aggregate(fns ...AggregateFunc) *BlockTimelineSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *BlockTimelineSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*BlockTimelineQuery, *BlockTimelineSelect](ctx, _s.BlockTimelineQuery, _s, _s.inters, v)
}

func (_s *BlockTimelineSelect) sqlScan(ctx context.Context, root *BlockTimelineQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
