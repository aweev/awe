// Code generated by ent, DO NOT EDIT.

package ent

import (
	"awe/ent/ent/predicate"
	"awe/ent/ent/userprofile"
	"awe/ent/ent/volunteerassignment"
	"awe/ent/ent/volunteeropportunity"
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// VolunteerAssignmentQuery is the builder for querying VolunteerAssignment entities.
type VolunteerAssignmentQuery struct {
	config
	ctx             *QueryContext
	order           []volunteerassignment.OrderOption
	inters          []Interceptor
	predicates      []predicate.VolunteerAssignment
	withOpportunity *VolunteerOpportunityQuery
	withVolunteer   *UserProfileQuery
	withFKs         bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the VolunteerAssignmentQuery builder.
func (_q *VolunteerAssignmentQuery) Where(ps ...predicate.VolunteerAssignment) *VolunteerAssignmentQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *VolunteerAssignmentQuery) Limit(limit int) *VolunteerAssignmentQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *VolunteerAssignmentQuery) Offset(offset int) *VolunteerAssignmentQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *VolunteerAssignmentQuery) Unique(unique bool) *VolunteerAssignmentQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *VolunteerAssignmentQuery) Order(o ...volunteerassignment.OrderOption) *VolunteerAssignmentQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryOpportunity chains the current query on the "opportunity" edge.
func (_q *VolunteerAssignmentQuery) QueryOpportunity() *VolunteerOpportunityQuery {
	query := (&VolunteerOpportunityClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(volunteerassignment.Table, volunteerassignment.FieldID, selector),
			sqlgraph.To(volunteeropportunity.Table, volunteeropportunity.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, volunteerassignment.OpportunityTable, volunteerassignment.OpportunityColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryVolunteer chains the current query on the "volunteer" edge.
func (_q *VolunteerAssignmentQuery) QueryVolunteer() *UserProfileQuery {
	query := (&UserProfileClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(volunteerassignment.Table, volunteerassignment.FieldID, selector),
			sqlgraph.To(userprofile.Table, userprofile.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, volunteerassignment.VolunteerTable, volunteerassignment.VolunteerColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first VolunteerAssignment entity from the query.
// Returns a *NotFoundError when no VolunteerAssignment was found.
func (_q *VolunteerAssignmentQuery) First(ctx context.Context) (*VolunteerAssignment, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{volunteerassignment.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *VolunteerAssignmentQuery) FirstX(ctx context.Context) *VolunteerAssignment {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first VolunteerAssignment ID from the query.
// Returns a *NotFoundError when no VolunteerAssignment ID was found.
func (_q *VolunteerAssignmentQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{volunteerassignment.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *VolunteerAssignmentQuery) FirstIDX(ctx context.Context) int {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single VolunteerAssignment entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one VolunteerAssignment entity is found.
// Returns a *NotFoundError when no VolunteerAssignment entities are found.
func (_q *VolunteerAssignmentQuery) Only(ctx context.Context) (*VolunteerAssignment, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{volunteerassignment.Label}
	default:
		return nil, &NotSingularError{volunteerassignment.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *VolunteerAssignmentQuery) OnlyX(ctx context.Context) *VolunteerAssignment {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only VolunteerAssignment ID in the query.
// Returns a *NotSingularError when more than one VolunteerAssignment ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *VolunteerAssignmentQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{volunteerassignment.Label}
	default:
		err = &NotSingularError{volunteerassignment.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *VolunteerAssignmentQuery) OnlyIDX(ctx context.Context) int {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of VolunteerAssignments.
func (_q *VolunteerAssignmentQuery) All(ctx context.Context) ([]*VolunteerAssignment, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*VolunteerAssignment, *VolunteerAssignmentQuery]()
	return withInterceptors[[]*VolunteerAssignment](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *VolunteerAssignmentQuery) AllX(ctx context.Context) []*VolunteerAssignment {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of VolunteerAssignment IDs.
func (_q *VolunteerAssignmentQuery) IDs(ctx context.Context) (ids []int, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(volunteerassignment.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *VolunteerAssignmentQuery) IDsX(ctx context.Context) []int {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *VolunteerAssignmentQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*VolunteerAssignmentQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *VolunteerAssignmentQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *VolunteerAssignmentQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *VolunteerAssignmentQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the VolunteerAssignmentQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *VolunteerAssignmentQuery) Clone() *VolunteerAssignmentQuery {
	if _q == nil {
		return nil
	}
	return &VolunteerAssignmentQuery{
		config:          _q.config,
		ctx:             _q.ctx.Clone(),
		order:           append([]volunteerassignment.OrderOption{}, _q.order...),
		inters:          append([]Interceptor{}, _q.inters...),
		predicates:      append([]predicate.VolunteerAssignment{}, _q.predicates...),
		withOpportunity: _q.withOpportunity.Clone(),
		withVolunteer:   _q.withVolunteer.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithOpportunity tells the query-builder to eager-load the nodes that are connected to
// the "opportunity" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *VolunteerAssignmentQuery) WithOpportunity(opts ...func(*VolunteerOpportunityQuery)) *VolunteerAssignmentQuery {
	query := (&VolunteerOpportunityClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withOpportunity = query
	return _q
}

// WithVolunteer tells the query-builder to eager-load the nodes that are connected to
// the "volunteer" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *VolunteerAssignmentQuery) WithVolunteer(opts ...func(*UserProfileQuery)) *VolunteerAssignmentQuery {
	query := (&UserProfileClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withVolunteer = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Status string `json:"status,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.VolunteerAssignment.Query().
//		GroupBy(volunteerassignment.FieldStatus).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *VolunteerAssignmentQuery) GroupBy(field string, fields ...string) *VolunteerAssignmentGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &VolunteerAssignmentGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = volunteerassignment.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Status string `json:"status,omitempty"`
//	}
//
//	client.VolunteerAssignment.Query().
//		Select(volunteerassignment.FieldStatus).
//		Scan(ctx, &v)
func (_q *VolunteerAssignmentQuery) Select(fields ...string) *VolunteerAssignmentSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &VolunteerAssignmentSelect{VolunteerAssignmentQuery: _q}
	sbuild.label = volunteerassignment.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a VolunteerAssignmentSelect configured with the given aggregations.
func (_q *VolunteerAssignmentQuery) Aggregate(fns ...AggregateFunc) *VolunteerAssignmentSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *VolunteerAssignmentQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !volunteerassignment.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *VolunteerAssignmentQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*VolunteerAssignment, error) {
	var (
		nodes       = []*VolunteerAssignment{}
		withFKs     = _q.withFKs
		_spec       = _q.querySpec()
		loadedTypes = [2]bool{
			_q.withOpportunity != nil,
			_q.withVolunteer != nil,
		}
	)
	if _q.withOpportunity != nil || _q.withVolunteer != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, volunteerassignment.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*VolunteerAssignment).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &VolunteerAssignment{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withOpportunity; query != nil {
		if err := _q.loadOpportunity(ctx, query, nodes, nil,
			func(n *VolunteerAssignment, e *VolunteerOpportunity) { n.Edges.Opportunity = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withVolunteer; query != nil {
		if err := _q.loadVolunteer(ctx, query, nodes, nil,
			func(n *VolunteerAssignment, e *UserProfile) { n.Edges.Volunteer = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *VolunteerAssignmentQuery) loadOpportunity(ctx context.Context, query *VolunteerOpportunityQuery, nodes []*VolunteerAssignment, init func(*VolunteerAssignment), assign func(*VolunteerAssignment, *VolunteerOpportunity)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*VolunteerAssignment)
	for i := range nodes {
		if nodes[i].volunteer_opportunity_assignments == nil {
			continue
		}
		fk := *nodes[i].volunteer_opportunity_assignments
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(volunteeropportunity.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "volunteer_opportunity_assignments" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *VolunteerAssignmentQuery) loadVolunteer(ctx context.Context, query *UserProfileQuery, nodes []*VolunteerAssignment, init func(*VolunteerAssignment), assign func(*VolunteerAssignment, *UserProfile)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*VolunteerAssignment)
	for i := range nodes {
		if nodes[i].user_profile_assignments == nil {
			continue
		}
		fk := *nodes[i].user_profile_assignments
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(userprofile.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_profile_assignments" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (_q *VolunteerAssignmentQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *VolunteerAssignmentQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(volunteerassignment.Table, volunteerassignment.Columns, sqlgraph.NewFieldSpec(volunteerassignment.FieldID, field.TypeInt))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, volunteerassignment.FieldID)
		for i := range fields {
			if fields[i] != volunteerassignment.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *VolunteerAssignmentQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(volunteerassignment.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = volunteerassignment.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// VolunteerAssignmentGroupBy is the group-by builder for VolunteerAssignment entities.
type VolunteerAssignmentGroupBy struct {
	selector
	build *VolunteerAssignmentQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *VolunteerAssignmentGroupBy) Aggregate(fns ...AggregateFunc) *VolunteerAssignmentGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *VolunteerAssignmentGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*VolunteerAssignmentQuery, *VolunteerAssignmentGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *VolunteerAssignmentGroupBy) sqlScan(ctx context.Context, root *VolunteerAssignmentQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// VolunteerAssignmentSelect is the builder for selecting fields of VolunteerAssignment entities.
type VolunteerAssignmentSelect struct {
	*VolunteerAssignmentQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *VolunteerAssignmentSelect) Aggregate(fns ...AggregateFunc) *VolunteerAssignmentSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *VolunteerAssignmentSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*VolunteerAssignmentQuery, *VolunteerAssignmentSelect](ctx, _s.VolunteerAssignmentQuery, _s, _s.inters, v)
}

func (_s *VolunteerAssignmentSelect) sqlScan(ctx context.Context, root *VolunteerAssignmentQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
